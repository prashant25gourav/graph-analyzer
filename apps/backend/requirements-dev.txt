# Development / test dependencies. Not required to run the app in production.
# Install with: pip install -r requirements-dev.txt (includes requirements.txt)
-r requirements.txt
pytest==8.4.1
httpx==0.28.1
